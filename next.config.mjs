/** @type {import('next').NextConfig} */
const nextConfig = {
    // 2) Enable the styled-components SWC transform
    compiler: {
        styledComponents: true,
    },
};

export default nextConfig;
